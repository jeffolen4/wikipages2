$(document).ready(function() {

  // var commitButtons = $("[name^='commit']");
  //
  // var submitButtons = $("[type^='submit']");
  //
  //
  // commitButtons.each( function (idx, val) {
  //   if ( !$(val).hasClass("btn-primary") ) {
  //     $(val).addClass("btn btn-primary");
  //   }
  // });
  //
  // submitButtons.each( function (idx, val) {
  //   if ( !$(val).hasClass("btn-primary") ) {
  //     $(val).addClass("btn btn-primary");
  //   }
  // });

  // var redirect_to = function (url_for) {
  //   window.location.href = url_for
  // };

})
