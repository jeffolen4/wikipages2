<div class="container">
  <div class="row">

    <h1>Editing Address</h1>

    <%= render 'form' %>

    <%= link_to 'Show', person_address_path(@address.person_id, @address.id) %> |
    <%= link_to 'Back', person_path(@address.person_id) %>
  </div>
</div>
