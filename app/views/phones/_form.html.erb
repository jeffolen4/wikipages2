<%= form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <input type="hidden" name="phone[person_id]" value="<%= @phone.person_id %>"></input>
    <div class="form-group"
      <%= f.label :phone_type %>
      <%= f.text_field :phone_type %>
    </div>
    <div class="form-group"
      <%= f.label :phone_number %>
      <%= f.text_field :phone_number %>
    </div>
    <%= f.submit %>
  </div>
<% end %>
